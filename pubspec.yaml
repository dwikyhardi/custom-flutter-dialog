name: d_dialog
description: Dialog material widget with blur background and popup animation
version: 1.0.0+1
homepage: https://github.com/dwikyhardi/custom-flutter-dialog


environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^2.0.1